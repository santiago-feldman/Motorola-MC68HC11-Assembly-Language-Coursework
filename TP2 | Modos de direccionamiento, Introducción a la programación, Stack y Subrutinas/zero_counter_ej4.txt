        p68h11                  ;indica el modelo del microprocesador

        org     $2000
        ldaa    $2018           ;carga la palabra elegida en el registro a
        ldab    #8
        stab    $3000           ;se crea el contador de bits
        clrb                    ;se crea el contador de ceros
loop    asla
        bcs     decount         ;si hay un 1 en el bit decrementa el contador de bits
        incb                    ;si hay un 0 incrementa el contador de ceros
decount dec     $3000
        bne     loop            ;si el contador de bits no es 0 pasa al siguiente bit
store   stab    $2019           ;si es cero guarda la cantidad de ceros contados
fin     jmp     fin

        END
